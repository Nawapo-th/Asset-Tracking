# การตั้งค่า HTTPS สำหรับการใช้กล้องบนมือถือ

## ปัญหา
เบราว์เซอร์สมัยใหม่ (Chrome, Safari, Firefox) ไม่อนุญาตให้เข้าถึงกล้องผ่าน HTTP เพื่อความปลอดภัย
- ✅ อนุญาตเฉพาะ `localhost` (สำหรับการพัฒนา)
- ❌ ไม่อนุญาตผ่าน IP address หรือ network อื่นๆ ผ่าน HTTP
- ✅ ต้องใช้ HTTPS เท่านั้น

## วิธีแก้ไข

### วิธีที่ 1: ใช้ HTTPS ด้วย Self-Signed Certificate (แนะนำ) ⭐

ระบบได้ตั้งค่าให้รองรับ HTTPS แล้ว!

#### 1. ตรวจสอบว่ามี Certificate Files
```bash
# ตรวจสอบว่ามีไฟล์ cert.pem และ key.pem หรือไม่
ls cert.pem key.pem
```

#### 2. เริ่มต้น Server
```bash
# ถ้าใช้ Docker
docker-compose up -d

# ถ้าใช้ Node.js โดยตรง
npm start
```

Server จะเปิด 2 ports:
- **HTTP**: `http://10.67.3.111:3000` (ใช้งานปกติ แต่เปิดกล้องไม่ได้บนมือถือ)
- **HTTPS**: `https://10.67.3.111:3443` (ใช้สำหรับเปิดกล้องบนมือถือ) 🔒

#### 3. เข้าถึงผ่าน HTTPS บนมือถือ

1. เปิดเบราว์เซอร์บนมือถือ
2. ไปที่ `https://10.67.3.111:3443`
3. **จะมีคำเตือน "Your connection is not private"** (เพราะใช้ self-signed certificate)
4. กด **"Advanced"** → **"Proceed to 10.67.3.111 (unsafe)"**
5. ตอนนี้สามารถใช้กล้องได้แล้ว! 📸

#### 4. QR Code
QR Code ที่สร้างใหม่จะชี้ไปที่ HTTPS URL โดยอัตโนมัติ

### วิธีที่ 2: สร้าง Certificate ใหม่ (ถ้าไม่มี cert.pem และ key.pem)

```bash
# สร้าง self-signed certificate (valid 365 days)
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes -subj "/CN=10.67.3.111"
```

### วิธีที่ 3: ใช้ ngrok (สำหรับ Testing)

ถ้าไม่ต้องการจัดการ certificate:

```bash
# Install ngrok
# Download from https://ngrok.com/download

# Run ngrok
ngrok http 3000
```

ngrok จะให้ HTTPS URL ที่สามารถใช้งานได้ทันที เช่น `https://abc123.ngrok.io`

## การทดสอบ

### ทดสอบบน Desktop
```
http://localhost:3000        ← ใช้งานปกติ
https://localhost:3443       ← HTTPS (ต้อง accept certificate warning)
```

### ทดสอบบนมือถือ
```
https://10.67.3.111:3443     ← ใช้ URL นี้เพื่อเปิดกล้อง
```

## หมายเหตุ

1. **Self-signed certificate** จะมีคำเตือนความปลอดภัยทุกครั้ง แต่ปลอดภัยสำหรับใช้ภายใน network
2. **Production**: ควรใช้ certificate จาก CA ที่เชื่อถือได้ (เช่น Let's Encrypt)
3. **Firewall**: ตรวจสอบว่า port 3443 เปิดอยู่
4. **IP Address**: เปลี่ยน `10.67.3.111` เป็น IP ของ server จริงๆ ถ้าต่างกัน

## Troubleshooting

### ปัญหา: ยังเปิดกล้องไม่ได้
- ✅ ตรวจสอบว่าใช้ **HTTPS** URL (`https://...`)
- ✅ ตรวจสอบว่ากด "Proceed" ผ่านคำเตือน certificate แล้ว
- ✅ ตรวจสอบว่าเบราว์เซอร์ขออนุญาตเข้าถึงกล้อง (ดูที่ address bar)

### ปัญหา: Connection refused
- ✅ ตรวจสอบว่า server กำลังรันอยู่
- ✅ ตรวจสอบว่า port 3443 เปิดอยู่
- ✅ ตรวจสอบ firewall settings

### ปัญหา: Certificate error
- ✅ ตรวจสอบว่ามีไฟล์ `cert.pem` และ `key.pem`
- ✅ สร้าง certificate ใหม่ด้วยคำสั่ง openssl ด้านบน
